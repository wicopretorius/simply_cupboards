node_modules/
dist/
.DS_Store
*.local

# env — never commit real secrets
.env
.env.local
.env.*.local

# Next.js
app/.next/
app/out/
app/.env.local
app/node_modules/

# Directus uploads (committed via snapshots only)
directus/uploads/
